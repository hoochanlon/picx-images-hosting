---
title: 创建一个 GitHub 仓库
---

# ① 第一步：创建一个 GitHub 仓库

首先，你需要在 GitHub 上创建一个新的仓库来存储图片。

:::note
仓库创建完成后，建议创建一个 Release 版本，这样可以使用更稳定的 CDN 链接。
:::

## Callout 示例（用于测试，可删除）：

:::note
这是一个 **Note** callout，用于一般性提示信息。
:::

:::tip
这是一个 **Tip** callout，用于提供有用的技巧和建议。
:::

:::info
这是一个 **Info** callout，用于提供信息（与 note 相同样式）。
:::

:::important
这是一个 **Important** callout，用于重要信息。
:::

:::warning
这是一个 **Warning** callout，用于警告信息。
:::

:::danger
这是一个 **Danger** callout，用于危险警告（与 caution 相同样式）。
:::

:::caution
这是一个 **Caution** callout，用于谨慎提示。
:::

:::note 自定义标题
这是带自定义标题的 Note callout。
:::

## 操作步骤：

1. 登录你的 GitHub 账号
2. 点击右上角的 "+" 号，选择 "New repository"
3. 填写仓库名称（例如：picx-images-hosting）
4. 选择仓库可见性（Public 或 Private）
5. 点击 "Create repository" 创建仓库

## 创建 Release（可选但推荐）：

1. 进入你刚创建的仓库
2. 点击右侧的 "Releases" 链接
3. 点击 "Create a new release"
4. 输入版本号（例如：1.0）
5. 点击 "Publish release"

```
仓库创建完成后，记下你的仓库地址，格式为：
https://github.com/你的用户名/仓库名
```

