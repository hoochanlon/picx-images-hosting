# 环境变量配置示例
# 在 Vercel 项目设置中添加这些环境变量
# 或在本地开发时创建 .env.local 文件（需要配置构建工具支持）

# ============================================
# 必需配置
# ============================================

# GitHub Personal Access Token (必需)
# 用于访问 GitHub API，需要 repo 权限
# 获取方式：GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
GH_TOKEN=your_github_personal_access_token_here

# ============================================
# 安全配置（强烈推荐）
# ============================================

# GitHub OAuth 配置（推荐使用，更安全）
# 如果配置了 GitHub OAuth，将使用 GitHub 官方 OAuth 认证
# 否则回退到密码认证（PASSWORD）
# 详细配置指南：docs/github-oauth-setup.md
GITHUB_OAUTH_CLIENT_ID=your_github_oauth_client_id
GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret
GITHUB_OAUTH_REDIRECT_URI=https://your-vercel-app.vercel.app/api/github-oauth?action=callback
GITHUB_REPO_OWNER=your_github_username
GITHUB_REPO_NAME=your_repo_name

# API 密钥 (可选，用于后端验证，作为备用方案)
# 如果设置了此值，后端 API 会验证所有写操作（上传、删除）的请求
# 建议使用强密码，长度至少 32 个字符
# 生成方式：可以使用在线工具生成随机字符串，或使用命令：
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_SECRET=your_strong_api_secret_here_min_32_chars

# ============================================
# 可选配置
# ============================================

# CORS 允许的域名 (可选)
# 用逗号分隔多个域名，如果不设置则从 api-config.json 读取
# 示例：ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
# ALLOWED_ORIGINS=

